---
description: Plan + write reclaim events to your Google Calendar (asks before writing)
---

Use the `reclaim-sync` subagent. Fetch this week's events, run the planner, show me the proposal, ask before writing, then create the approved events on my primary calendar with a `[Reclaim]` prefix.
