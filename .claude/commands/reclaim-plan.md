---
description: Run the Reclaim planner against your real calendar and propose a schedule
---

Run the Reclaim sync agent to fetch this week's events from Google Calendar, plan against `config/`, and show me the proposed schedule. Do NOT write any events without explicit confirmation.

Use the `reclaim-sync` subagent.
